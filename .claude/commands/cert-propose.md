# /cert-propose — RENAMED
# This skill has been renamed to /cortex-propose to follow the naming convention.
#
# cert-* = governance checks (cert-enforce, cert-verify, cert-precheck)
# cortex-* = deliverable generators (cortex-propose, cortex-build, cortex-design-handoff)
#
# Use /cortex-propose instead.

**This skill is now /cortex-propose. Use that instead.**

```
Old: /cert-propose new
New: /cortex-propose new
```

See: C:\luv\Cortex\skills\cortex-propose.md
