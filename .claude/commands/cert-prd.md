# /cert-prd — RENAMED
# This skill has been renamed to /cortex-prd to follow the naming convention.
#
# cert-* = governance checks (cert-enforce, cert-verify, cert-precheck)
# cortex-* = deliverable generators (Generator, not governance)
#
# Use /cortex-prd instead.

**This skill is now /cortex-prd. Use that instead.**

```
Old: /cert-prd
New: /cortex-prd
```

See: C:\luv\Cortex\skills$new.md
