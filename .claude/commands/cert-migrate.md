# /cert-migrate — RENAMED
# This skill has been renamed to /cortex-migrate to follow the naming convention.
#
# cert-* = governance checks (cert-enforce, cert-verify, cert-precheck)
# cortex-* = deliverable generators (Executor, not governance)
#
# Use /cortex-migrate instead.

**This skill is now /cortex-migrate. Use that instead.**

```
Old: /cert-migrate
New: /cortex-migrate
```

See: C:\luv\Cortex\skills$new.md
