# /cert-discover — RENAMED
# This skill has been renamed to /cortex-discover to follow the naming convention.
#
# cert-* = governance checks (cert-enforce, cert-verify, cert-precheck)
# cortex-* = deliverable generators (Scanner/generator, not a governance gate)
#
# Use /cortex-discover instead.

**This skill is now /cortex-discover. Use that instead.**

```
Old: /cert-discover
New: /cortex-discover
```

See: C:\luv\Cortex\skills$new.md
