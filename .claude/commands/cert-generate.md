# /cert-generate — RENAMED
# This skill has been renamed to /cortex-generate to follow the naming convention.
#
# cert-* = governance checks (cert-enforce, cert-verify, cert-precheck)
# cortex-* = deliverable generators (Generator, obviously)
#
# Use /cortex-generate instead.

**This skill is now /cortex-generate. Use that instead.**

```
Old: /cert-generate
New: /cortex-generate
```

See: C:\luv\Cortex\skills$new.md
