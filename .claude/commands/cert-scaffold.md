# /cert-scaffold — RENAMED
# This skill has been renamed to /cortex-scaffold to follow the naming convention.
#
# cert-* = governance checks (cert-enforce, cert-verify, cert-precheck)
# cortex-* = deliverable generators (Scaffolder, not governance)
#
# Use /cortex-scaffold instead.

**This skill is now /cortex-scaffold. Use that instead.**

```
Old: /cert-scaffold
New: /cortex-scaffold
```

See: C:\luv\Cortex\skills$new.md
