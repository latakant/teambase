# /cert-build — RENAMED
# This skill has been renamed to /cortex-build to follow the naming convention.
#
# cert-* = governance checks (cert-enforce, cert-verify, cert-precheck)
# cortex-* = domain builders and generators (cortex-build, cortex-design)
#
# Use /cortex-build instead.

**This skill is now /cortex-build. Use that instead.**

```
Old: /cert-build auth
New: /cortex-build auth
```

See: C:\luv\Cortex\skills\cortex-build.md
