# /cert-docs — RENAMED
# This skill has been renamed to /cortex-docs to follow the naming convention.
#
# cert-* = governance checks (cert-enforce, cert-verify, cert-precheck)
# cortex-* = deliverable generators (Generator, not governance)
#
# Use /cortex-docs instead.

**This skill is now /cortex-docs. Use that instead.**

```
Old: /cert-docs
New: /cortex-docs
```

See: C:\luv\Cortex\skills$new.md
