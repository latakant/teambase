# /cert-spec — RENAMED
# This skill has been renamed to /cortex-spec to follow the naming convention.
#
# cert-* = governance checks (cert-enforce, cert-verify, cert-precheck)
# cortex-* = deliverable generators (Spec generator, not governance)
#
# Use /cortex-spec instead.

**This skill is now /cortex-spec. Use that instead.**

```
Old: /cert-spec
New: /cortex-spec
```

See: C:\luv\Cortex\skills$new.md
