# /cert-extract — RENAMED
# This skill has been renamed to /cortex-extract to follow the naming convention.
#
# cert-* = governance checks (cert-enforce, cert-verify, cert-precheck)
# cortex-* = deliverable generators (Generator, not governance)
#
# Use /cortex-extract instead.

**This skill is now /cortex-extract. Use that instead.**

```
Old: /cert-extract
New: /cortex-extract
```

See: C:\luv\Cortex\skills$new.md
